fix: Update installer to create proper plugin directory structure

- Fix installer to create .obsidian/plugins/longneobsidian-vault-sync/ directory
- Place scripts in both vault root (for detection) and plugin directory (for settings)
- Add manifest.json with proper plugin metadata and configuration
- Add basic main.js for Obsidian plugin functionality with sync command
- Update test function to verify both vault root and plugin directory structure
- Enhance installation output to show plugin directory creation

Directory Structure After Install:
vault-root/
├── sync-vault-coordinated.sh          ← For plugin detection
├── sync-vault.sh                       ← For plugin execution  
└── .obsidian/plugins/longneobsidian-vault-sync/
    ├── manifest.json                   ← Plugin metadata
    ├── main.js                         ← Plugin code
    ├── README.md                       ← Documentation
    ├── sync-vault-coordinated.sh       ← Backup/reference
    └── sync-vault.sh                   ← Backup/reference

This ensures both:
✅ Obsidian sync plugins can detect scripts (vault root location)
✅ Plugin settings and configuration have proper directory structure
✅ Plugin can be properly installed and managed within Obsidian

Fixes the issue where installer only created vault root scripts but missing
the plugin directory structure needed for proper Obsidian plugin integration.
